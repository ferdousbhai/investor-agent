# Cloudflare Workers uses bindings (KV, Durable Objects) instead of env vars.
# See wrangler.jsonc for binding configuration.
#
# For local development with `wrangler dev`, bindings are simulated automatically.
# No .env file is needed for this project.
