# TODO

- [ ] Set `CLOUDFLARE_API_TOKEN` secret in GitHub repo Settings → Secrets → Actions
- [ ] Create KV namespace: `wrangler kv namespace create CACHE`, then paste the returned ID into `wrangler.jsonc`
